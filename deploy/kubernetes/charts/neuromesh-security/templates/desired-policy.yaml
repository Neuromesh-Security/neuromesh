{{- if .Values.desiredPolicy.enabled }}
{{/*
DesiredPolicy ConfigMap + PE watch-only RBAC (parity with neuromesh-desired-policy.yaml).

PE ServiceAccount READ (sole ConfigMap grant for neuromesh-zt-policy-engine):
  get+watch on resourceNames=[.Values.desiredPolicy.name] — no list, no writes.
WRITE stays with operators / GitOps / CI apply jobs (cluster or namespace admin
bindings), not this Role. Live gate: manual_verify_desired_policy_dynamic.sh
scenario 2.
*/}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.desiredPolicy.name }}
  namespace: {{ include "neuromesh.namespace" . }}
  labels:
    app.kubernetes.io/name: neuromesh-zt-policy-engine
    app.kubernetes.io/component: desired-policy
    app.kubernetes.io/part-of: neuromesh
  annotations:
    neuromesh.security/desired-policy-plane: bundle-only-pr1
    neuromesh.security/rego-coupling: "blocked-until-pr2"
data:
  policy.json: |
{{ .Values.desiredPolicy.config.policyJson | indent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: neuromesh-zt-policy-engine-desired-policy
  namespace: {{ include "neuromesh.namespace" . }}
  labels:
    app.kubernetes.io/name: neuromesh-zt-policy-engine
    app.kubernetes.io/component: desired-policy
    app.kubernetes.io/part-of: neuromesh
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: [{{ .Values.desiredPolicy.name | quote }}]
    verbs: ["get", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: neuromesh-zt-policy-engine-desired-policy
  namespace: {{ include "neuromesh.namespace" . }}
  labels:
    app.kubernetes.io/name: neuromesh-zt-policy-engine
    app.kubernetes.io/component: desired-policy
    app.kubernetes.io/part-of: neuromesh
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: neuromesh-zt-policy-engine-desired-policy
subjects:
  - kind: ServiceAccount
    name: neuromesh-zt-policy-engine
    namespace: {{ include "neuromesh.namespace" . }}
{{- end }}
