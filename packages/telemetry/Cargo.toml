[package]
name = "neuromesh-telemetry"
version.workspace = true
edition.workspace = true
description = "Standard MetricEvent contract for Neuromesh observability and Kafka pipelines"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0"
